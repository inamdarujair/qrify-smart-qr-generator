/**
 * Hero Section featuring a large modern typographic gradient header and product tagline.
 */
export const Hero = () => {
  return (
    <header className="hero-section" aria-label="Hero Introduction">
      <h1 className="hero-title">QRify</h1>
      <p className="hero-subtitle">
        Generate beautiful QR codes in seconds — for links, text, contacts, and WiFi.
      </p>
    </header>
  );
};

export default Hero;
